# Swift build artifacts
.build/
*.o
*.d

# Built app bundles — must be built locally (pre-built binaries
# downloaded from GitHub are blocked by macOS Gatekeeper because they
# are not notarized).
TinyRecorder/TinyRecorder.app/

# Xcode
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/

# macOS
.DS_Store
